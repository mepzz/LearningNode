const add = require('./utils.js') //The require command lets the program know
// that another .Js file will be used.

const sum = add(4, -2) //Runs the add function that is in the other Js file
console.log(sum) //Outputs the sum.